{
	"name": "wheres-is-shedletsky",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"check-types": "turbo run check-types",
		"dev": "turbo run dev",
		"lint": "biome check --fix --reporter=concise"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.6",
		"turbo": "^2.10.7",
		"vitest": "^4.1.10"
	},
	"engines": {
		"node": ">=18"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.17.0",
			"onFail": "download"
		}
	}
}
